name: dev_quiz
description: DevQuiz
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.0.0
  share_plus: ^2.0.1
  flutter_mobx: ^2.0.0
  mobx: ^2.0.1
  dio: ^4.0.0
  http_mock_adapter: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.1
  mobx_codegen: ^2.0.1+3  
  mockito: ^5.0.5

flutter:
  assets:
    - assets/images/
    - assets/database/
  uses-material-design: true
